// date commands

const DATE_CURRENT = 'current';
const DATE_ADD = 'add';
const DATE_SUB = 'sub';
// ---
// common
const EMPTY = '';
const SELF_ARG = '$0';
const COMMANDS_ARG = '_';
const NO_COMMAND_FOUND = 'Sorry, but no command found';
// ---
// types
const NUMBER_TYPE = 'number';

module.exports = {
    DATE_CURRENT,
    DATE_ADD,
    DATE_SUB,
    EMPTY,
    NO_COMMAND_FOUND,
    SELF_ARG,
    COMMANDS_ARG,
    NUMBER_TYPE,
};